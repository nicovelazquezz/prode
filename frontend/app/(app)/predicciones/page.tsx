/**
 * Placeholder de /predicciones — la implementacion real llega en
 * Phase 5. Por ahora solo confirma que el (app) layout monta y el
 * guard funciona.
 */
export default function PrediccionesPage() {
  return (
    <div className="mx-auto max-w-2xl px-4 py-6">
      <h1 className="font-display text-3xl md:text-4xl font-black uppercase tracking-wide text-[var(--color-prode-near-black)]">
        Mis predicciones
      </h1>
      <p className="mt-3 font-sans text-sm text-[var(--color-prode-text-secondary)]">
        En construcción — Phase 5 implementa la lista de partidos por fase.
      </p>
    </div>
  );
}
